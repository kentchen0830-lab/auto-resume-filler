# Contributing

欢迎提交修复和改进。提交前请：

1. 不要包含真实简历、账号、Cookie、API Key 或浏览器 Profile。
2. 新增站点适配时避免依赖不可解释的硬编码个人数据。
3. 对字段匹配、敏感信息过滤或 AI 输出解析的修改补充测试。
4. 保留“用户确认后填写、用户自行最终提交”的安全边界。
5. 运行 `npm run check` 和 `npm test`。
